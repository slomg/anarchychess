export const DEFAULT_USERNAME = "luka";
export const DEFAULT_PASSWORD = "securePassword123";

export const enum NavbarItem {
    home = "/",
    play = "/play",
    profile = "/user",
    settings = "/settings/profile",
    logout = "/logout",

    login = "/login",
    signup = "/signup",
}
